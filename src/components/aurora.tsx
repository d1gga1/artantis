"use client";

/**
 * Nome storico dello sfondo animato, tenuto solo per compatibilità:
 * il vero sfondo ora è `living-background.tsx`. Si può eliminare senza
 * conseguenze — non è importato da nessuna pagina.
 */
export { LivingBackground as Aurora } from "@/components/living-background";
